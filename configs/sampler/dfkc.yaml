# @package _global_
name: dfkc
sampler:
  _target_: genmol.samplers.DFKCSampler
  num_particles: 8
  mode: reward
  beta: 2.0
  beta_schedule: constant
  ess_threshold: 0.5
  resample_strategy: systematic
  seed: null
  verbose: false
